需要实现的能力：AE 网络模块 加密库的选择与加密解密

能力：
1）在 UDP/TCP 连接成功后，加密模块进行协商
2）发送方 把本地支持的加密类型、协议版本 通过 AETlv 的格式传给对方
3）接收方根据收到的 加密类型进行选择
4）交互密钥信息
5）生成对称密钥
6）后期使用对称密钥进行加解密，发送：加密，接收：解密
7）需要支持的加密类型： ECC 、RSA


文件：

AENetSecruity 对 AESession 需要发送的数据进行一次性加密
